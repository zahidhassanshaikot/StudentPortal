@model IEnumerable<StudentPortal.Models.Result>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>View Student Results</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<p>

    @using (Html.BeginForm("Index","Result",FormMethod.Get))
    {
        <b>Search By:  </b>@Html.RadioButton("searchBy", "StudentId", true)<text>StudentId</text>
        @Html.RadioButton("searchBy","Subject")<text>Subject</text>
        @Html.TextBox("search")<input type="submit" value="Search" />      
        
    }


</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Student.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mark)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Student.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mark)
        </td>

        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ResultId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ResultId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ResultId })
        </td>*@
    </tr>
}

</table>
